export enum StickyNoteType {
  EVENT = 'event',
  COMMAND = 'command',
  CONSTRAINT = 'constraint',
  SYSTEM = 'system',
  POLICY = 'policy',
  QUERY_MODEL = 'query-model',
  ACTOR = 'actor',
  HOTSPOT = 'hotspot',
}
